# Environment variables configuration example for LLM and SQLChain project

# OpenAI API configuration
# Required for LLM class when using OpenAI
OPENAI_API_KEY=sk-your-openai-api-key-here

# Ollama API configuration (optional)
# Use this if you want to run with local models via Ollama
OLLAMA_URL=http://localhost:11434/v1

# Database connection parameters
# Required for SQLChain class
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=yourusername
DB_PASSWORD=yourpassword
DB_DATABASE=yourdatabase

# Model configuration (optional, defaults available in code)
# LLM_DEFAULT_MODEL=gpt-4o

# Parallel processing configuration (optional)
# WORKERS=4
# CHUNK_SIZE=10